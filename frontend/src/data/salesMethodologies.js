// Sales methodology content (body + objections, per formation) used to live here as
// static exports, but is now stored in Supabase (table `sales_methodologies`) and edited
// directly from the Méthodologie page in the app, including the objection that used to be
// hardcoded as "common" (Samdi/Dimanch) — it's now a regular per-course objection like any
// other. See MethodologyPage.js.
